<!DOCTYPE html>
<html lang="en">
<head>
  <title>Select Theme</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(to right, #ff7e5f, #feb47b);
      height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
    }

    .theme-container {
      background-color: rgba(255, 255, 255, 0.8);
      padding: 30px;
      border-radius: 10px;
      box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
      text-align: center;
    }
  </style>
</head>
<body>
  <div class="theme-container">
    <h2>Select Your Theme</h2>
    <form method="POST">
      <div class="form-check">
        <input class="form-check-input" type="radio" name="theme" value="animals" required>
        <label class="form-check-label">Animals</label>
      </div>
      <div class="form-check">
        <input class="form-check-input" type="radio" name="theme" value="nature">
        <label class="form-check-label">Nature</label>
      </div>
      <div class="form-check">
        <input class="form-check-input" type="radio" name="theme" value="movies">
        <label class="form-check-label">Movies</label>
      </div>
      <button class="btn btn-success mt-3">Start Game</button>
    </form>
  </div>
</body>
</html>
